const {userSeed} = require('./users.seeder')
const {roleSeed} = require('./roles.seeder')
const {plantSeed} = require('./plants.seeder')
const {locationSeed} = require('./location.seeder')
const {area1Seed} = require('./area1.seeder')
const {area2Seed} = require('./area2.seeder')
module.exports = {
    userSeed,
    roleSeed,
    plantSeed,
    locationSeed,
    area1Seed,
    area2Seed,
}